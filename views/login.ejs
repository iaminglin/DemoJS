<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
    <meta charset="utf-8"/>
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.css"/>
    <style>
        body{
            padding:20px;
        }
    </style>
</head>
<body>
<form method="post" action = "/users/login">
    <div class="form-group">
        <label>用户名称</label>
        <input name="userName"  placeholder="请输入名称">
    </div>
    <div class="form-group">
        <label >用户密码</label>
        <input name="password"   placeholder="请输入密码">
    </div>
    <div>
        <%if(typeof isPass !== "undefined" && isPass == false){%>
            <label><font color="#FF0000">用户名或密码错误</font></label>
        <%}%>
    </div>
    <button type="submit" class="btn btn-default">登录</button>
</form>
</body>
</html>